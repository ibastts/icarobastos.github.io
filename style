:root {
  --bg: #070d18;
  --bg-alt: #0d1726;
  --surface: rgba(15, 23, 36, 0.9);
  --surface-strong: rgba(17, 28, 42, 0.96);
  --surface-border: rgba(120, 180, 255, 0.18);
  --accent: #66e9ff;
  --accent-strong: #2bc7ff;
  --accent-dim: #0d8bbd;
  --accent-glow: rgba(102, 233, 255, 0.35);
  --text: #edf6ff;
  --text-muted: #a7b9cf;
  --text-faint: #7386a0;
  --shadow: rgba(5, 10, 18, 0.4);
  --font-display: 'Space Grotesk', sans-serif;
  --font-body: 'Inter', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}

* { box-sizing: border-box; }

html, body {
  margin: 0;
  padding: 0;
  min-height: 100vh;
  background:
    radial-gradient(circle at top left, rgba(28, 92, 150, 0.28), transparent 32%),
    radial-gradient(circle at bottom right, rgba(55, 214, 255, 0.12), transparent 25%),
    linear-gradient(180deg, var(--bg) 0%, var(--bg-alt) 100%);
  color: var(--text);
  font-family: var(--font-body);
}

body {
  position: relative;
}

.scanlines {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 50;
  background: repeating-linear-gradient(
    to bottom,
    rgba(255, 255, 255, 0.010) 0,
    rgba(255, 255, 255, 0.010) 1px,
    transparent 1px,
    transparent 3px
  );
  mix-blend-mode: screen;
}

.topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 24px;
  border-bottom: 1px solid var(--surface-border);
  background: rgba(7, 13, 24, 0.85);
  backdrop-filter: blur(10px);
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--text-muted);
  letter-spacing: 0.04em;
  position: sticky;
  top: 0;
  z-index: 10;
}

.topbar-left { display: flex; align-items: center; gap: 8px; }

.dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--accent);
  box-shadow: 0 0 8px var(--accent-glow);
}

.topbar-right {
  color: var(--accent);
  opacity: 0.9;
}

.boot {
  min-height: calc(100vh - 48px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
}

#bootLog {
  font-family: var(--font-mono);
  font-size: 13px;
  color: var(--accent);
  white-space: pre-wrap;
  line-height: 1.8;
  max-width: 560px;
  text-shadow: 0 0 20px rgba(102, 233, 255, 0.22);
}

.app {
  display: grid;
  grid-template-columns: 320px 1fr;
  gap: 28px;
  max-width: 1180px;
  margin: 0 auto;
  padding: 40px 24px 80px;
}

@media (max-width: 860px) {
  .app { grid-template-columns: 1fr; }
}

.profile-card,
.panel {
  position: relative;
  background: linear-gradient(180deg, rgba(18, 28, 42, 0.95), rgba(12, 18, 28, 0.9));
  border: 1px solid var(--surface-border);
  border-radius: 18px;
  box-shadow: 0 14px 32px var(--shadow);
  backdrop-filter: blur(10px);
  transform: translateY(0) scale(1);
  transition: transform 420ms cubic-bezier(0.22, 1, 0.36, 1), box-shadow 420ms ease, border-color 300ms ease, padding 300ms ease;
  overflow: hidden;
}

.profile-card::before,
.panel::before {
  content: "";
  position: absolute;
  inset: -1px;
  background: linear-gradient(120deg, transparent 0%, rgba(102, 233, 255, 0.18) 38%, transparent 70%);
  transform: translateX(-120%);
  transition: transform 700ms ease;
  pointer-events: none;
}

.profile-card:hover::before,
.panel:hover::before {
  transform: translateX(120%);
}

.profile-card {
  padding: 28px 24px;
  height: fit-content;
  position: sticky;
  top: 82px;
}

.profile-card:hover,
.panel:hover {
  transform: translateY(-4px) scale(1.005);
  border-color: rgba(102, 233, 255, 0.42);
  box-shadow: 0 18px 32px rgba(5, 10, 18, 0.45);
}

.avatar {
  width: 62px;
  height: 62px;
  border-radius: 16px;
  background: linear-gradient(135deg, var(--accent), var(--accent-strong));
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 20px;
  color: #061018;
  margin-bottom: 16px;
  box-shadow: 0 12px 24px rgba(43, 199, 255, 0.25);
}

.name {
  font-family: var(--font-display);
  font-size: 24px;
  font-weight: 700;
  margin: 0 0 4px;
  letter-spacing: -0.02em;
}

.role {
  color: var(--accent);
  font-family: var(--font-mono);
  font-size: 13px;
  margin: 0 0 6px;
}

.location {
  color: var(--text-muted);
  font-size: 13px;
  margin: 0 0 16px;
}

.status-pill {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(102, 233, 255, 0.08);
  border: 1px solid rgba(102, 233, 255, 0.2);
  padding: 7px 12px;
  border-radius: 999px;
  font-size: 11px;
  font-family: var(--font-mono);
  color: var(--accent);
  margin-bottom: 18px;
}

.pulse {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--accent);
  box-shadow: 0 0 8px var(--accent-glow);
  animation: pulse 1.8s infinite ease-in-out;
}

@keyframes pulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.45; transform: scale(0.92); }
}

.summary {
  font-size: 13.5px;
  line-height: 1.7;
  color: var(--text-muted);
  margin: 0 0 20px;
}

.contact {
  display: flex;
  flex-direction: column;
  gap: 10px;
  padding-top: 16px;
  border-top: 1px solid var(--surface-border);
}

.contact-link {
  color: var(--text);
  text-decoration: none;
  font-size: 13px;
  font-family: var(--font-mono);
  transition: color 0.2s ease, transform 0.2s ease;
  width: fit-content;
}

.contact-link:hover {
  color: var(--accent);
  transform: translateX(2px);
}

.main-col {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.panel {
  padding: 20px 22px 24px;
}

.skill-panel {
  --rx: 0deg;
  --ry: 0deg;
  --lift: 0px;
  --panel-scale: 1;
  transform-style: preserve-3d;
  perspective: 1200px;
  transform: perspective(1100px) rotateX(var(--rx)) rotateY(var(--ry)) translateY(var(--lift)) scale(var(--panel-scale));
  will-change: transform, box-shadow;
  transition: transform 420ms cubic-bezier(0.22, 1, 0.36, 1), box-shadow 420ms ease, border-color 300ms ease;
}

.skill-panel:hover {
  --panel-scale: 1.008;
  --lift: -2px;
  border-color: rgba(102, 233, 255, 0.38);
}

.skill-panel .skill-list,
.skill-panel .skill-group {
  transform: translateZ(18px);
  transition: transform 420ms ease;
}

.panel-title {
  font-family: var(--font-mono);
  font-size: 12px;
  letter-spacing: 0.08em;
  color: var(--accent);
  text-transform: uppercase;
  margin: 0 0 20px;
  padding-bottom: 12px;
  border-bottom: 1px solid var(--surface-border);
}

.skill-group { margin-bottom: 18px; }
.skill-group:last-child { margin-bottom: 0; }

.skill-group-title {
  font-size: 12px;
  color: var(--text-faint);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin: 0 0 12px;
}

.skill-list { display: flex; flex-direction: column; gap: 10px; }

.skill-row { display: flex; flex-direction: column; gap: 5px; }

.skill-label {
  display: flex;
  justify-content: space-between;
  gap: 10px;
  font-size: 13px;
  color: var(--text);
  line-height: 1.5;
}

.skill-label span:last-child {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--text-muted);
  white-space: nowrap;
}

.skill-bar-track {
  height: 6px;
  background: rgba(255, 255, 255, 0.06);
  border-radius: 999px;
  overflow: hidden;
}

.skill-bar-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent-dim), var(--accent));
  border-radius: 999px;
  width: 0%;
  transition: width 1s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 0 12px var(--accent-glow);
}

.timeline {
  display: flex;
  flex-direction: column;
  gap: 22px;
  position: relative;
}

.timeline-item {
  position: relative;
  padding-left: 22px;
  border-left: 1px solid var(--surface-border);
}

.timeline-item::before {
  content: '';
  position: absolute;
  left: -4.5px;
  top: 4px;
  width: 9px;
  height: 9px;
  border-radius: 50%;
  background: var(--accent);
  box-shadow: 0 0 8px var(--accent-glow);
}

.timeline-head {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 8px;
  margin-bottom: 4px;
}

.timeline-role {
  font-weight: 600;
  font-size: 14.5px;
}

.timeline-company {
  color: var(--accent);
  font-size: 13px;
}

.timeline-period {
  font-family: var(--font-mono);
  font-size: 11.5px;
  color: var(--text-muted);
  white-space: nowrap;
}

.timeline-meta {
  font-size: 12px;
  color: var(--text-faint);
  margin-bottom: 10px;
}

.timeline-highlights {
  margin: 0;
  padding-left: 18px;
  color: var(--text-muted);
  font-size: 13px;
  line-height: 1.7;
}

.project-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap: 14px;
}

.project-card {
  border: 1px solid var(--surface-border);
  border-radius: 12px;
  padding: 16px;
  background: rgba(255, 255, 255, 0.02);
  transition: border-color 0.2s ease, transform 0.2s ease, box-shadow 0.2s ease;
}

.project-card:hover {
  border-color: rgba(102, 233, 255, 0.45);
  transform: translateY(-2px);
  box-shadow: 0 10px 24px rgba(8, 18, 28, 0.35);
}

.project-name {
  font-weight: 600;
  font-size: 14px;
  margin: 0 0 6px;
  display: flex;
  align-items: center;
  gap: 6px;
}

.project-name::before {
  content: '◈';
  color: var(--accent);
  font-size: 12px;
}

.project-desc {
  font-size: 12.5px;
  color: var(--text-muted);
  line-height: 1.6;
  margin: 0 0 12px;
}

.project-link {
  display: inline-block;
  color: var(--accent);
  text-decoration: none;
  font-family: var(--font-mono);
  font-size: 11px;
  letter-spacing: 0.02em;
}

.project-link:hover { text-decoration: underline; }

.edu-item {
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 8px;
  padding: 4px 0;
}

.edu-degree { font-weight: 600; font-size: 14px; }
.edu-inst { color: var(--accent); font-size: 13px; }
.edu-period {
  font-family: var(--font-mono);
  font-size: 11.5px;
  color: var(--text-muted);
}

@media (max-width: 640px) {
  .topbar { padding: 12px 16px; }
  .app { padding: 24px 16px 48px; }
  .profile-card, .panel { border-radius: 14px; }
  .panel { padding: 22px 18px; }
  .timeline-head, .edu-item { flex-direction: column; }
}

@keyframes revealCard {
  from {
    opacity: 0;
    transform: translateY(14px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.profile-card,
.panel {
  animation: revealCard 640ms ease both;
}

.panel:nth-child(2) { animation-delay: 80ms; }
.panel:nth-child(3) { animation-delay: 140ms; }
.panel:nth-child(4) { animation-delay: 200ms; }
.panel:nth-child(5) { animation-delay: 260ms; }
